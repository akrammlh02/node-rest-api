<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Certificate of Completion - DevAcademy</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;600;700;900&family=Great+Vibes&family=Montserrat:wght@300;400;500;600&family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&display=swap"
    rel="stylesheet">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: #f0f2f5;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      font-family: 'Montserrat', sans-serif;
      padding: 20px;
    }

    /* 
      Certificate Wrapper 
      Dimensions: A4 Landscape (297mm x 210mm)
    */
    .certificate-wrapper {
      width: 297mm;
      height: 210mm;
      background: #fff;
      position: relative;
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      color: #1a1a1a;
    }

    /* 
      The Main Border Frame 
    */
    .certificate-frame {
      position: absolute;
      top: 10mm;
      left: 10mm;
      right: 10mm;
      bottom: 10mm;
      border: 2px solid #1e3a8a;
      /* Deep Blue */
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      padding: 15mm;
      background-image:
        radial-gradient(circle at center, rgba(30, 58, 138, 0.03) 0%, transparent 70%);
    }

    /* Inner Gold Border */
    .certificate-frame::before {
      content: '';
      position: absolute;
      top: 4px;
      left: 4px;
      right: 4px;
      bottom: 4px;
      border: 1px solid #d4af37;
      /* Gold */
      pointer-events: none;
    }

    /* Corner Accents */
    .corner {
      position: absolute;
      width: 40px;
      height: 40px;
      border: 4px solid #1e3a8a;
      z-index: 2;
    }

    .corner-tl {
      top: -2px;
      left: -2px;
      border-right: none;
      border-bottom: none;
    }

    .corner-tr {
      top: -2px;
      right: -2px;
      border-left: none;
      border-bottom: none;
    }

    .corner-bl {
      bottom: -2px;
      left: -2px;
      border-right: none;
      border-top: none;
    }

    .corner-br {
      bottom: -2px;
      right: -2px;
      border-left: none;
      border-top: none;
    }

    /* Header Section */
    .header {
      text-align: center;
      width: 100%;
    }

    .logo {
      font-family: 'Cinzel', serif;
      font-weight: 900;
      font-size: 28px;
      color: #1e3a8a;
      letter-spacing: 4px;
      margin-bottom: 20px;
      text-transform: uppercase;
    }

    .main-title {
      font-family: 'Cinzel', serif;
      font-size: 48px;
      font-weight: 700;
      color: #111;
      letter-spacing: 6px;
      margin: 0;
      line-height: 1;
      text-transform: uppercase;
    }

    .sub-title {
      font-family: 'Montserrat', sans-serif;
      font-size: 14px;
      text-transform: uppercase;
      letter-spacing: 3px;
      color: #d4af37;
      /* Gold */
      margin-top: 10px;
      font-weight: 600;
    }

    /* Content Section */
    .content {
      text-align: center;
      width: 100%;
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .presented-to {
      font-family: 'Playfair Display', serif;
      font-style: italic;
      font-size: 18px;
      color: #666;
      margin-bottom: 10px;
    }

    .student-name {
      font-family: 'Great Vibes', cursive;
      font-size: 80px;
      color: #1e3a8a;
      margin: 10px 0;
      line-height: 1.2;
      padding: 0 20px;
      text-shadow: 1px 1px 0 rgba(0, 0, 0, 0.1);
    }

    .description {
      font-size: 16px;
      color: #444;
      margin-bottom: 15px;
      max-width: 80%;
      margin-left: auto;
      margin-right: auto;
      line-height: 1.6;
    }

    .course-title {
      font-family: 'Playfair Display', serif;
      font-size: 32px;
      font-weight: 700;
      color: #111;
      margin: 10px 0 20px;
      border-bottom: 2px solid #d4af37;
      display: inline-block;
      padding-bottom: 10px;
    }

    /* Footer Section */
    .footer {
      width: 100%;
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      padding: 0 40px;
      margin-top: 20px;
    }

    .signature-block {
      text-align: center;
      width: 200px;
    }

    .sign-img {
      height: 50px;
      margin-bottom: 5px;
      /* Placeholder for signature image */
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: 'Great Vibes', cursive;
      font-size: 30px;
      color: #1e3a8a;
    }

    .sign-line {
      height: 1px;
      background: #ccc;
      width: 100%;
      margin-bottom: 8px;
    }

    .sign-name {
      font-weight: 700;
      font-size: 14px;
      color: #111;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .sign-title {
      font-size: 11px;
      color: #888;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    /* Seal */
    .seal-container {
      position: absolute;
      bottom: 30mm;
      left: 50%;
      transform: translateX(-50%);
      width: 120px;
      height: 120px;
    }

    .seal {
      width: 100%;
      height: 100%;
      background: #d4af37;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
      position: relative;
    }

    .seal::after {
      content: '';
      position: absolute;
      top: 4px;
      left: 4px;
      right: 4px;
      bottom: 4px;
      border: 2px dashed #fff;
      border-radius: 50%;
    }

    .seal-inner {
      color: #fff;
      font-weight: 900;
      text-transform: uppercase;
      font-size: 14px;
      text-align: center;
      line-height: 1.2;
      letter-spacing: 1px;
    }

    /* ID */
    .cert-id {
      position: absolute;
      bottom: 5mm;
      right: 10mm;
      font-size: 10px;
      color: #aaa;
      font-family: monospace;
    }

    /* Download Button */
    .btn-container {
      margin-bottom: 30px;
      text-align: center;
    }

    .download-btn {
      background: #1e3a8a;
      color: white;
      border: none;
      padding: 15px 40px;
      font-size: 16px;
      font-weight: 600;
      border-radius: 50px;
      cursor: pointer;
      box-shadow: 0 4px 15px rgba(30, 58, 138, 0.4);
      transition: transform 0.2s, box-shadow 0.2s;
      display: inline-flex;
      align-items: center;
      gap: 10px;
      font-family: 'Montserrat', sans-serif;
    }

    .download-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(30, 58, 138, 0.6);
      background: #172554;
    }

    .download-btn:active {
      transform: translateY(0);
    }

    .download-btn svg {
      width: 20px;
      height: 20px;
      fill: currentColor;
    }

    /* Hide button when printing manually */
    @media print {
      body {
        background: none;
        padding: 0;
      }

      .btn-container {
        display: none;
      }

      .certificate-wrapper {
        box-shadow: none;
        margin: 0;
        page-break-after: always;
      }
    }
  </style>
</head>

<body>

  <div class="btn-container">
    <button id="downloadPDF" class="download-btn">
      <svg viewBox="0 0 24 24">
        <path d="M19 9h-4V3H9v6H5l7 7 7-7zM5 18v2h14v-2H5z" />
      </svg>
      Download PDF
    </button>
  </div>

  <div class="certificate-wrapper" id="certificate">
    <div class="certificate-frame">
      <!-- Corners -->
      <div class="corner corner-tl"></div>
      <div class="corner corner-tr"></div>
      <div class="corner corner-bl"></div>
      <div class="corner corner-br"></div>

      <!-- Header -->
      <div class="header">
        <div class="logo">DevAcademy</div>
        <h1 class="main-title">Certificate of Completion</h1>
        <div class="sub-title">This certifies that</div>
      </div>

      <!-- Content -->
      <div class="content">
        <div class="student-name">{{clientName}}</div>
        <div class="description">
          has successfully completed all requirements for the course
        </div>
        <div class="course-title">{{courseTitle}}</div>
        <div class="description">
          demonstrating excellence, dedication, and mastery of the subject matter.
        </div>
      </div>

      <!-- Seal -->
      <div class="seal-container">
        <div class="seal">
          <div class="seal-inner">
            OFFICIAL<br>CERTIFIED
          </div>
        </div>
      </div>

      <!-- Footer -->
      <div class="footer">
        <div class="signature-block">
          <div class="sign-img">Akram M.</div>
          <div class="sign-line"></div>
          <div class="sign-name">Akram Melihi</div>
          <div class="sign-title">Instructor & Founder</div>
        </div>

        <div class="signature-block">
          <div class="sign-img">{{issueDate}}</div>
          <div class="sign-line"></div>
          <div class="sign-name">Date Issued</div>
          <div class="sign-title">DevAcademy Official</div>
        </div>
      </div>

      <div class="cert-id">ID: {{certificateId}}</div>
    </div>
  </div>

  <script>
    document.getElementById('downloadPDF').addEventListener('click', function () {
      const element = document.getElementById('certificate');
      const btn = this;

      // Visual feedback
      const originalText = btn.innerHTML;
      btn.innerHTML = 'Generating...';
      btn.style.opacity = '0.7';
      btn.disabled = true;

      // PDF Options
      const opt = {
        margin: 0,
        filename: 'DevAcademy_Certificate.pdf',
        image: { type: 'jpeg', quality: 1 },
        html2canvas: {
          scale: 2,
          useCORS: true,
          logging: true,
          letterRendering: true,
          allowTaint: true
        },
        jsPDF: {
          unit: 'mm',
          format: 'a4',
          orientation: 'landscape'
        }
      };

      // Generate
      html2pdf().set(opt).from(element).save().then(() => {
        btn.innerHTML = originalText;
        btn.style.opacity = '1';
        btn.disabled = false;
      }).catch(err => {
        console.error('PDF Generation Error:', err);
        alert('Error generating PDF. Please try again.');
        btn.innerHTML = originalText;
        btn.style.opacity = '1';
        btn.disabled = false;
      });
    });
  </script>
</body>

</html>