<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>DevAcademy - Programming Quiz</title>
  <link rel="stylesheet" href="/style.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
  <link rel="icon" type="image/x-icon" href="/images/logo.png">
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@100;200;300;400;500;600;700;800;900&display=swap"
    rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css">
  <style>
    :root {
      --primary-gradient: linear-gradient(135deg, #7b09cd 0%, #9d4edd 100%);
      --glass-bg: rgba(255, 255, 255, 0.9);
      --glass-border: rgba(255, 255, 255, 0.2);
    }

    * {
      font-family: 'Outfit', sans-serif;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background: #f0f2f5;
      min-height: 100vh;
      color: #1a1a1a;
    }

    .quiz-wrapper {
      max-width: 1200px;
      margin: 2rem auto;
      padding: 0 2rem;
    }

    .screen {
      display: none;
      animation: fadeIn 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .screen.active {
      display: block;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(30px) scale(0.98);
      }

      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }

    /* Hero Section */
    .quiz-hero {
      text-align: center;
      padding: 4rem 0;
      background: var(--primary-gradient);
      border-radius: 30px;
      color: white;
      margin-bottom: 3rem;
      box-shadow: 0 20px 40px rgba(123, 9, 205, 0.2);
      position: relative;
      overflow: hidden;
    }

    .quiz-hero::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 60%);
      pointer-events: none;
    }

    .welcome-title {
      font-size: 3.5rem;
      font-weight: 800;
      margin-bottom: 1rem;
      letter-spacing: -1px;
    }

    .welcome-subtitle {
      font-size: 1.25rem;
      opacity: 0.9;
      max-width: 600px;
      margin: 0 auto 2.5rem;
    }

    /* Search & Filter */
    .filter-section {
      background: white;
      padding: 1.5rem;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
      margin-bottom: 2.5rem;
      display: flex;
      gap: 1.5rem;
      align-items: center;
      flex-wrap: wrap;
    }

    .search-box {
      flex: 1;
      position: relative;
      min-width: 300px;
    }

    .search-box input {
      width: 100%;
      padding: 1rem 1.5rem 1rem 3.5rem;
      border: 2px solid #f1f5f9;
      border-radius: 15px;
      font-size: 1rem;
      transition: all 0.3s;
      background: #f8fafc;
    }

    .search-box input:focus {
      outline: none;
      border-color: #7b09cd;
      background: white;
      box-shadow: 0 0 0 4px rgba(123, 9, 205, 0.1);
    }

    .search-box .material-symbols-outlined {
      position: absolute;
      left: 1.2rem;
      top: 50%;
      transform: translateY(-50%);
      color: #94a3b8;
    }

    .category-filters {
      display: flex;
      gap: 0.75rem;
      overflow-x: auto;
      padding-bottom: 5px;
    }

    .category-btn {
      padding: 0.75rem 1.5rem;
      border-radius: 12px;
      border: 2px solid #f1f5f9;
      background: white;
      color: #64748b;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
      white-space: nowrap;
    }

    .category-btn.active {
      background: #7b09cd;
      color: white;
      border-color: #7b09cd;
      box-shadow: 0 8px 15px rgba(123, 9, 205, 0.2);
    }

    /* Language Cards */
    .languages-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
      gap: 2rem;
    }

    .language-card {
      background: white;
      border-radius: 24px;
      padding: 2rem;
      text-align: center;
      transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      border: 1px solid #f1f5f9;
      cursor: pointer;
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }

    .language-card:hover {
      transform: translateY(-12px);
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.08);
      border-color: #7b09cd;
    }

    .lang-icon-container {
      width: 80px;
      height: 80px;
      background: #f8fafc;
      border-radius: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-bottom: 1.5rem;
      transition: all 0.3s;
    }

    .language-card:hover .lang-icon-container {
      background: rgba(123, 9, 205, 0.1);
      transform: scale(1.1) rotate(5deg);
    }

    .language-icon-emoji {
      font-size: 3rem;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .language-icon-emoji i {
      font-size: 3.5rem;
      transition: transform 0.3s ease;
    }

    .language-name {
      font-size: 1.25rem;
      font-weight: 700;
      color: #1e293b;
      margin-bottom: 0.5rem;
    }

    .resume-badge {
      font-size: 0.8rem;
      font-weight: 600;
      color: #7b09cd;
      background: rgba(123, 9, 205, 0.1);
      padding: 4px 12px;
      border-radius: 20px;
      margin-top: 0.5rem;
    }

    /* Quiz Container */
    .quiz-container {
      background: white;
      border-radius: 30px;
      padding: 3rem;
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.06);
    }

    .quiz-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 3rem;
    }

    .progress-bar-wrapper {
      background: #f1f5f9;
      height: 10px;
      border-radius: 20px;
      width: 250px;
      margin-top: 0.5rem;
    }

    .progress-bar-fill {
      background: var(--primary-gradient);
      height: 100%;
      border-radius: 20px;
      transition: width 0.6s ease;
      box-shadow: 0 0 15px rgba(123, 9, 205, 0.3);
    }

    .question-number {
      font-weight: 700;
      color: #7b09cd;
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 1rem;
    }

    .question-text {
      font-size: 1.75rem;
      font-weight: 700;
      color: #1e293b;
      line-height: 1.4;
      margin-bottom: 2.5rem;
    }

    .options-grid {
      display: grid;
      gap: 1.25rem;
    }

    .option-button {
      padding: 1.25rem 2rem;
      border: 2px solid #f1f5f9;
      border-radius: 20px;
      background: white;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 1.25rem;
      transition: all 0.3s;
      text-align: left;
    }

    .option-letter {
      width: 45px;
      height: 45px;
      background: #f1f5f9;
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      color: #64748b;
      transition: all 0.3s;
    }

    .option-button:hover:not(.disabled) {
      border-color: #7b09cd;
      background: #fdfaff;
      transform: translateX(10px);
    }

    .option-button:hover:not(.disabled) .option-letter {
      background: #7b09cd;
      color: white;
    }

    .option-button.correct {
      background: #ecfdf5;
      border-color: #10b981;
    }

    .option-button.correct .option-letter {
      background: #10b981;
      color: white;
    }

    .option-button.incorrect {
      background: #fef2f2;
      border-color: #ef4444;
    }

    .option-button.incorrect .option-letter {
      background: #ef4444;
      color: white;
    }

    /* Live Stats Card */
    .live-stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      margin-bottom: 3rem;
    }

    .live-stat {
      background: #f8fafc;
      padding: 1rem;
      border-radius: 15px;
      text-align: center;
    }

    .stat-label {
      font-size: 0.8rem;
      font-weight: 600;
      color: #94a3b8;
      display: block;
      margin-bottom: 0.25rem;
    }

    .stat-value {
      font-size: 1.25rem;
      font-weight: 700;
      color: #1e293b;
    }

    /* Buttons */
    .btn-primary-custom {
      background: var(--primary-gradient);
      color: white;
      padding: 1rem 2.5rem;
      border-radius: 15px;
      font-weight: 700;
      border: none;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 0.75rem;
      transition: all 0.3s;
      box-shadow: 0 10px 20px rgba(123, 9, 205, 0.2);
    }

    .btn-primary-custom:hover {
      transform: scale(1.05);
      box-shadow: 0 15px 30px rgba(123, 9, 205, 0.3);
    }

    .btn-secondary-custom {
      background: #f1f5f9;
      color: #64748b;
      padding: 1rem 2rem;
      border-radius: 15px;
      font-weight: 700;
      border: none;
      cursor: pointer;
      transition: all 0.3s;
    }

    .btn-secondary-custom:hover {
      background: #e2e8f0;
      color: #1e293b;
    }

    /* Results */
    .results-card {
      background: white;
      border-radius: 30px;
      padding: 4rem;
      text-align: center;
      box-shadow: 0 30px 60px rgba(0, 0, 0, 0.1);
    }

    .score-percentage {
      font-size: 5rem;
      font-weight: 900;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 1rem;
    }

    /* Animations */
    .hidden {
      display: none;
    }

    @media (max-width: 991px) {
      .welcome-title {
        font-size: 3rem;
      }

      .quiz-hero {
        padding: 3rem 1.5rem;
      }
    }

    @media (max-width: 768px) {
      .quiz-wrapper {
        padding: 0 1rem;
        margin: 1rem auto;
      }

      .welcome-title {
        font-size: 2.2rem;
        letter-spacing: -0.5px;
      }

      .welcome-subtitle {
        font-size: 1.1rem;
        margin-bottom: 2rem;
      }

      .quiz-hero {
        padding: 2.5rem 1rem;
        border-radius: 20px;
        margin-bottom: 2rem;
      }

      .filter-section {
        padding: 1rem;
        gap: 1rem;
        border-radius: 15px;
      }

      .search-box {
        min-width: 100%;
      }

      .search-box input {
        padding: 0.8rem 1rem 0.8rem 3rem;
        font-size: 0.95rem;
      }

      .category-filters {
        width: 100%;
        justify-content: flex-start;
      }

      .category-btn {
        padding: 0.6rem 1.2rem;
        font-size: 0.9rem;
      }

      .languages-grid {
        grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
        gap: 1rem;
      }

      .language-card {
        padding: 1.5rem 1rem;
        border-radius: 20px;
      }

      .lang-icon-container {
        width: 60px;
        height: 60px;
        margin-bottom: 1rem;
      }

      .language-icon-emoji i {
        font-size: 2.5rem;
      }

      .language-name {
        font-size: 1.1rem;
      }

      .quiz-container {
        padding: 1.5rem;
        border-radius: 20px;
      }

      .quiz-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 1.5rem;
        margin-bottom: 2rem;
      }

      .progress-bar-wrapper {
        width: 100%;
      }

      .live-stats {
        width: 100%;
        margin-bottom: 0;
      }

      .question-text {
        font-size: 1.3rem;
        margin-bottom: 2rem;
      }

      .option-button {
        padding: 1rem;
        border-radius: 15px;
        gap: 1rem;
      }

      .option-letter {
        width: 38px;
        height: 38px;
        font-size: 0.9rem;
      }

      .score-percentage {
        font-size: 3.5rem;
      }

      .results-card {
        padding: 2.5rem 1.5rem;
      }

      .btn-primary-custom,
      .btn-secondary-custom {
        padding: 0.8rem 1.5rem;
        font-size: 0.95rem;
      }
    }

    @media (max-width: 480px) {
      .welcome-title {
        font-size: 1.8rem;
      }

      .languages-grid {
        grid-template-columns: repeat(2, 1fr);
      }

      .live-stats {
        grid-template-columns: repeat(2, 1fr);
      }

      .live-stat:last-child {
        grid-column: span 2;
      }

      .question-text {
        font-size: 1.15rem;
      }

      .results-card .row>div {
        width: 100%;
      }

      .results-card .d-flex {
        flex-direction: column;
        width: 100%;
      }

      .results-card .btn-primary-custom,
      .results-card .btn-secondary-custom,
      #nextBtn {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>

<body>
  <header class="main-header">
    <div class="header-container">
      <nav class="navbar">
        <!-- Logo -->
        <a href="/" class="brand-logo" aria-label="DevAcademy Home">
          <img src="/images/logo2-removebg-preview.png" alt="DevAcademy Logo">
          {{!-- <span class="brand-text">DevAcademy</span> --}}
        </a>

        <!-- Desktop Navigation -->
        <ul class="nav-links-desktop">
          <li><a href="/" class="nav-link">Home</a></li>
          <li><a href="/course" class="nav-link">Courses</a></li>
          <li><a href="/#interactive" class="nav-link">Code</a></li>
          <li><a href="/quiz" class="nav-link active">Quiz</a></li>
        </ul>

        <!-- Auth Buttons & Cart -->
        <div class="header-actions">
          <!-- Cart Link -->
          <a href="/payment/checkout" class="cart-link" title="Checkout">
            <span class="material-symbols-outlined">shopping_cart</span>
            <span class="cart-badge" id="cartBadgeNav" style="display: none;">0</span>
          </a>

          {{#if user}}
          <a href="/dashboard" class="action-btn dashboard-btn">
            <span class="material-symbols-outlined">dashboard</span>
            Dashboard
          </a>
          <a href="/logout" class="action-btn logout-btn">
            <span class="material-symbols-outlined">logout</span>
          </a>
          {{else}}
          <a href="/login" class="action-btn login-btn">Login</a>
          <a href="/signup" class="action-btn signup-btn">Get Started</a>
          {{/if}}

          <!-- Mobile Menu Trigger -->
          <button class="mobile-menu-btn" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasRight"
            aria-controls="offcanvasRight" aria-label="Open menu">
            <svg class="hamburger-icon" viewBox="0 0 24 24" width="32" height="32">
              <path d="M4 6h16M4 12h16M4 18h16" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" />
            </svg>
          </button>
        </div>
      </nav>
    </div>

    <!-- Mobile Menu Offcanvas -->
    <div class="offcanvas offcanvas-end custom-offcanvas" tabindex="-1" id="offcanvasRight"
      aria-labelledby="offcanvasRightLabel">
      <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="offcanvasRightLabel">Menu</h5>
        <button type="button" class="btn-close-custom" data-bs-dismiss="offcanvas" aria-label="Close">
          <span class="material-symbols-outlined">close</span>
        </button>
      </div>
      <div class="offcanvas-body">
        <div class="mobile-nav-links">
          <a href="/" class="mobile-link">
            <span class="material-symbols-outlined">home</span> Home
          </a>
          <a href="/course" class="mobile-link">
            <span class="material-symbols-outlined">school</span> Courses
          </a>
          <a href="/#interactive" class="mobile-link">
            <span class="material-symbols-outlined">code</span> Code Editor
          </a>
          <a href="/quiz" class="mobile-link active" style="color: #3f10fa; background: #f8fafc;">
            <span class="material-symbols-outlined">quiz</span> Quiz
          </a>
          <a href="/payment/checkout" class="mobile-link">
            <span class="material-symbols-outlined">shopping_cart</span> Cart
            <span class="badge bg-danger ms-2" id="cartBadgeMobile" style="font-size: 0.8em; display: none;">0</span>
          </a>
        </div>

        <div class="mobile-auth-actions">
          {{#if user}}
          <a href="/dashboard" class="mobile-btn dashboard">Dashboard</a>
          <a href="/logout" class="mobile-btn logout">Logout</a>
          {{else}}
          <a href="/login" class="mobile-btn login">Login</a>
          <a href="/signup" class="mobile-btn signup">Sign Up Free</a>
          {{/if}}
        </div>
      </div>
    </div>
  </header>

  <div class="quiz-wrapper">
    <!-- Welcome / Language Selection Screen -->
    <div id="welcomeScreen" class="screen active">
      <div class="quiz-hero">
        <h1 class="welcome-title">Test Your Skills</h1>
        <p class="welcome-subtitle">Master your favorite technologies through curated programming challenges.</p>

        {{#if user}}
        <p class="mb-0">Logged in as <strong>{{user.firstname}}</strong></p>
        {{else}}
        <a href="/login" class="btn btn-light rounded-pill px-4 py-2 fw-bold text-primary">Login to Save Progress</a>
        {{/if}}
      </div>

      <div class="filter-section">
        <div class="search-box">
          <span class="material-symbols-outlined">search</span>
          <input type="text" id="langSearch" placeholder="Search 50+ languages (e.g. JavaScript, Python, React...)"
            oninput="filterLanguages()">
        </div>
        <div class="category-filters">
          <button class="category-btn active" onclick="filterByCategory('all')">All</button>
          <button class="category-btn" onclick="filterByCategory('web')">Web</button>
          <button class="category-btn" onclick="filterByCategory('backend')">Backend</button>
          <button class="category-btn" onclick="filterByCategory('mobile')">Mobile</button>
        </div>
      </div>

      <div class="languages-grid" id="languagesGrid">
        {{#each languages}}
        <div class="language-card" data-language="{{language}}" onclick="selectLanguage('{{language}}')">
          <div class="lang-icon-container">
            <div class="language-icon-emoji">🚀</div>
          </div>
          <div class="language-name">{{language}}</div>
          <div id="resume-badge-{{language}}"></div>
        </div>
        {{/each}}

        {{#unless languages.length}}
        <div style="grid-column: 1/-1; text-align: center; padding: 5rem;">
          <span class="material-symbols-outlined" style="font-size: 5rem; color: #e2e8f0;">quiz</span>
          <p class="mt-3 text-muted">No quiz languages available yet.</p>
        </div>
        {{/unless}}
      </div>
    </div>

    <!-- Quiz Screen -->
    <div id="quizScreen" class="screen">
      <div class="quiz-container">
        <div class="quiz-header">
          <div>
            <div class="question-number" id="quizLanguage">Quiz</div>
            <div id="progressText" class="text-muted fw-bold">Question 0/0</div>
            <div class="progress-bar-wrapper">
              <div class="progress-bar-fill" id="progressBar" style="width: 0%"></div>
            </div>
          </div>
          <div id="liveStats" class="live-stats">
            <!-- Live stats will be inserted here -->
          </div>
        </div>

        <div class="question-wrapper" id="questionWrapper">
          <!-- Question will be inserted here -->
        </div>

        <div class="d-flex justify-content-between align-items: center; mt-5">
          <button class="btn-secondary-custom" onclick="exitQuiz()">
            <span class="material-symbols-outlined align-middle">logout</span>
            Exit
          </button>
          <button class="btn-secondary-custom" onclick="resetQuiz()">
            <span class="material-symbols-outlined align-middle">refresh</span>
            Restart
          </button>
        </div>
      </div>
    </div>

    <!-- Results Screen -->
    <div id="resultsScreen" class="screen">
      <div class="results-card">
        <div class="mb-4">
          <span class="material-symbols-outlined" style="font-size: 5rem; color: #7b09cd;">workspace_premium</span>
        </div>
        <h2 class="welcome-title mb-2">Quiz Finished!</h2>
        <p class="text-muted mb-4" id="resultsLanguage"></p>

        <div class="score-percentage" id="finalScore">0%</div>
        <p class="fw-bold text-uppercase letter-spacing-1 mb-5" style="color: #64748b;">Your Final Score</p>

        <div class="row g-4 mb-5">
          <div class="col-md-4">
            <div class="live-stat">
              <span class="stat-label">Correct</span>
              <span class="stat-value text-success" id="correctAnswers">0</span>
            </div>
          </div>
          <div class="col-md-4">
            <div class="live-stat">
              <span class="stat-label">Incorrect</span>
              <span class="stat-value text-danger" id="incorrectAnswers">0</span>
            </div>
          </div>
          <div class="col-md-4">
            <div class="live-stat">
              <span class="stat-label">Time</span>
              <span class="stat-value" id="statsTime">0m</span>
            </div>
          </div>
        </div>

        <div class="d-flex justify-content-center gap-3">
          <button class="btn-primary-custom" onclick="retakeQuiz()">
            Try Another Language
            <span class="material-symbols-outlined">arrow_forward</span>
          </button>
          <button class="btn-secondary-custom" onclick="location.href='/dashboard'">
            Dashboard
          </button>
        </div>
      </div>
    </div>

    <!-- Loading Screen -->
    <div id="loadingScreen" class="loading-spinner">
      <div class="spinner"></div>
      {{!-- <p class="fw-bold" style="color: #7b09cd;">Loading Quiz...</p> --}}
    </div>
  </div>

  <!-- Hidden data for JS -->
  <input type="hidden" id="userLoggedIn" value="{{#if user}}true{{else}}false{{/if}}">
  <div id="languagesData" data-languages='{{json languages}}' style="display:none;"></div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
  <script src="/js/cart-utils.js"></script>
  <script src="/quiz.js"></script>
</body>

</html>